# 工作日志 · 网页版修复记录

> 26/09/10 建立第一代版本

---

## 一、说明

本文件用于记录每个 part 需要编辑的错漏之处，例如部分代码被吞导致内容缺失或未加载、对照纸质书发现的识别错误和漏内容、勘误文档处的编辑。看到一个记一个，没看到不用管，不需要为此修改网页，直接在编辑处改着用。现在的网站只能算 demo，不用追求完美。这样也方便在不小心破坏格式或清除数据时，记录哪里更改过，后续补回。

## 二、版本与存储策略

目前 pure 网页为简单版本，不再持续更新，本地保存其原始代码。

full 网页的更新代码直接在 GitHub 编辑页完成，本地保存为第二个版本；1 个主页、8 个网页及 js 文件都未更新。等后续修复到一定阶段、不再更改后，再在本地保存文件。

> ！GitHub 那个文件夹下载下来的目录配置，本地也能用！如果需要对外分享就可以按文件分享而不是给网站，防止泄露了。

## 三、full 网页版已修复问题

### 26/09/11

（1）浏览器标签页添加"🌸"图标。

（2）主页卡片虚线深浅不一，统一改回浅色虚线，不再出现点过的链接变黑的情况。

（3）主页"学习进度"区域增加朗读功能，想听的知识点直接复制粘贴到这里即可收听。

### 26/09/12

（4）增加 JSON 导入/导出同步机制，手机、电脑可以轻松备份。

（5）修复笔记卡片交互：有笔记的卡片默认展开笔记；点击答案区时，答案与笔记一同显示；点击笔记图标只显示笔记，不显示答案。

（6）接入 AI 答疑助手，点击卡片时可自动识别卡片内容并回答。

（7）通过在 AI 系统提示词中设定编号样式，使分点回答不再空行；并优化 JSON 同步，使 AI 配置除 key 外都可同步。

### 26/09/13

（8）修复 AI 分点回答存在空行、间距偏大的问题。

（9）修复 AI 读卡片时只能看到题目和答案区，看不到笔记区和卡片上方自带小标签的问题。

（10）AI 思考过程中，按钮换成浅鹅黄色。

（11）修复手机端 AI 问题：微信内可正常使用，但不能拖拽为独立窗口；手机浏览器打开时无法显示设置卡片，也没有退出功能。

（12）微信无法导出 JSON，必须用手机浏览器打开才能下载；已增加提示和复制按钮。

（13）AI 设计：在复制按钮旁增加"重新生成"按钮和"删除"按钮，可在界面删除对话。

（14）AI 记忆轮数从 6 轮更新至 10 轮。

（15）退出 AI 界面后重新点回，仍停留在刚才的界面，而不是最后的位置。

（16）AI 界面增加类似 DeepSeek 的进度条，方便拖动查看某轮问答；同时处理 AI 输出过快导致手机界面上下横跳的问题。

（17）自动读取卡片时，切换卡片后记忆容易停留在上一道，需要引导才能回答下一道。现已更换为以卡片标签为索引识别，点击新题可直接基于本题回答。

（18）新增图片与附件上传功能。

（19）修复手机端 AI 提问框提示字符溢出问题。

（20）修复微信窗口复制 json 文件超出剪贴板记忆上限问题，已改成分段切分复制粘贴，并进行了统一的 json 文件压缩。

（21）修复同时打开笔记区和 AI 提问区粘贴图片会粘贴两边的问题。

（22）修复关于 AI 重新生成按钮在超过一定时间或轮次不再显示的问题。

### 26/09/14

（23）修复有的数字旁边有 `$200$` 这样货币符号的问题。

（24）微信浏览器在删掉两条以上的对话内容时，提示框有变化。本来是左边取消、右边确定，但是第三次操作就会出现左边是确定、右边是关闭网页，甚至"关闭网页"这四个字还是绿色的。已经统一全部改成自定义的卡片样式，暂定为左边为红色删除，右边取消。

（25）在导出数据加一个可以选择界面，选择是否导出 AI 聊天。

（26）微信切分感觉 4000 还少了，复制一个切分为 4000 的页面，最多显示 3818，而更多是 3600+ 的。已规定到 4400，帮助减少一些剪切次数。

（27）修复手机端 enter 键发送的问题，调整为换行，电脑端才是 enter 发送。

（28）AI 回答生成过程中，要求可以暂停，点击暂停后消息框下方有继续生成按钮，点击发送键也可再继续生成；此时输入框重新提问会截断上一次对话并进行新的回答。

（29）修复手机端点完"清除""同步"按钮后一直"粘"住不放的问题。

（30）主页"清除"和"同步"按钮外圈光晕粗细不一致，手机夜间模式下尤其明显，已统一。

（31）子页笔记区改成点开只看、点文本框才编辑，空笔记和有笔记的卡片行为一致。

（32）AI 窗口右侧进度条长了会被裁掉、早期消息点不回去，改成可以滚动。

（33）AI 对话可以分享链接，做成勾选式：只把勾选的那几条生成链接，没勾的不会进链接。

（34）分享链接打开是一个铺满整屏的 AI 对话窗口（手机电脑都自适应），不带出知识库网页；只能复制，不能重新生成、删除或继续提问，不会消耗 tokens；标签页显示"来自分享的对话"。

（35）8 个子页统一加上和主页一样的 🌸 图标；标签页标题去掉"南开经济学 · "前缀，统一为"微观背诵 · 记忆卡"这样的格式。

（36）手机端 AI 窗口的上传按钮点完后绿框一直不消失，要再点旁边才恢复。现在改成只有手指按下的瞬间有反馈，松手就恢复；顺带把 AI 面板里其它按钮的同类问题一起处理了。


（37）笔记里插入的图片会自动压缩后再保存；万一浏览器存储不够，会明确提示"没保存成功"，不会再一声不响丢掉。

（38）笔记改成自动保存，写一半关掉页面也会有提醒。

（39）分享链接万一在转发时被截断，打开时会有提示，不会默默变成普通页面。


（40）八个页面都加上了「自测模式」开关：打开后答案被遮住，先自己背，再点「查看答案」对照，然后自评「会了／模糊／忘了」，点同一个键可清除标记；统计条能按标记筛选，答案也能重新遮住反复默写。遮罩本身点不动，防止手机误触。关闭开关后卡片外观、按钮、提示全部恢复原样。标记存在各自的 selftest 里，会跟着学习数据一起导出。

（41）历年真题页没有答案区，所以那里不做遮答案，改用「错题本」模式：只做掌握度标记和筛选，作答区照常使用。

### 26/09/15

（42）清空笔记的确认弹窗改成了和删除对话、同步那里一样的自定义卡片样式，不再是浏览器自带的那种灰扑扑的弹出框。

（43）分享对话链接打开时不再弹出"是否保存密码"的提示（之前分享页里还带着密码输入框，被浏览器误当成要登录了）。

（44）分享对话里"取消"按钮颜色调深了，平时显示的颜色和鼠标放上去时一致，不再发白看不清。

（45）自测模式下，答案没被遮住的时候 AI 能读到答案，只有答案被遮罩时才读不到；另外 AI 现在会盯着我正在看的那张卡片，不会再出现"明明看到了却说没看到"的情况。

（46）手机端在 AI 窗口里上下滑动看上下文，退出后正在看的卡片位置不会乱跑了（之前在 AI 里滑动会把整个页面也带着滚，退出去卡片位置就变了）。

（47）修了所有子页里"小于号"被当成代码标签、导致后面整段文字消失的问题（比如"L_d<L*"后面那句话会被砍掉），8 个子页都排查过一遍。

（48）分享链接改短了，对方点开直接就是分享的对话界面，不再先跳到我的原网页再点"确认继续查看"；对方也只能看到分享的那段对话，看不到我整站的内容。打开分享对话默认拉到顶部从头看，不再一进去就到底部。

（49）修好了收藏和笔记按钮：之前批量把"清空笔记"的原生弹窗换成自定义弹窗时，把收藏按钮的处理器错接成了清空笔记（点收藏会问是否清空笔记），笔记按钮也失了响应、插图/保存/清空三个按钮的处理器全没了。现已把收藏（切换收藏态）、笔记（展开笔记区）、插图、保存、清空五个按钮的处理器都接回正确位置，8 个子页一致，清空弹窗只在点"🗑️ 清空"时出现。

（50）分享框的"取消"按钮之前悬停没任何可见反馈（颜色改了跟原本一样），现在给它加了和"全选/生成链接"一致的悬停效果：浅色背景 + 一点淡淡阴影，鼠标放上去能明显感觉到。

（51）本地从子页跳回主页时浏览器又弹"保存密码"。根因是配置里的 API Key 输入框是 type="password"，浏览器把它当登录密码、离开页面就提示保存。已把这个框改成普通文本框 + CSS 圆点遮罩（视觉上仍像密码框，但浏览器不再当凭据、不再弹保存），并加了几个密码管理器忽略属性兜底。分享视图里该框本来就会被移除，不受影响。


## 四、仍存在的问题

### 26/09/12

（1）手机端按钮 hover 粘滞问题（多处），如卡片的收藏笔记笔记按钮，自测模式的会了模糊按钮，搜索框等等，点击后产生触摸效果，但重新点击无法直接恢复样式，要点旁边才能恢复样式。均存在该问题。修复方法：用 `@media (hover: hover)` 包裹 hover 样式，仅对真正支持悬停的设备生效。已搁置。

### 26/09/13

（2）手机夜间模式主页"清除"和"同步"按钮存在外圈光晕粗细不一致的问题，应该是因为颜色反色的自带情况，没有做夜间模式适配。但不是很需要改，因为我一般不用夜间模式学习。

### 26/09/15

（3）分享对话的链接因为要带上完整对话内容，还是偏长，没能彻底缩短（上面第 48 条里改短的是链接前面的路径部分，核心对话内容长度不变），这点没完全解决。