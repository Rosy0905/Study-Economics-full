# 工作日志 · 网页版修复记录

> 26/09/10 建立第一代版本

---

## 一、说明

本文件用于记录每个 part 需要编辑的错漏之处，例如部分代码被吞导致内容缺失或未加载、对照纸质书发现的识别错误和漏内容、勘误文档处的编辑。看到一个记一个，没看到不用管，不需要为此修改网页，直接在编辑处改着用。现在的网站只能算 demo，不用追求完美。这样也方便在不小心破坏格式或清除数据时，记录哪里更改过，后续补回。

## 二、版本与存储策略

目前 pure 网页为简单版本，不再持续更新，本地保存其原始代码。

full 网页的更新代码直接在 GitHub 编辑页完成，本地保存为第二个版本；1 个主页、8 个网页及 js 文件都未更新。等后续修复到一定阶段、不再更改后，再在本地保存文件。

> ！GitHub 那个文件夹下载下来的目录配置，本地也能用！如果需要对外分享就可以按文件分享而不是给网站，防止泄露了。

## 三、full 网页版已修复问题

### 26/09/11

（1）浏览器标签页添加"🌸"图标。

（2）主页卡片虚线深浅不一，统一改回浅色虚线，不再出现点过的链接变黑的情况。

（3）主页"学习进度"区域增加朗读功能，想听的知识点直接复制粘贴到这里即可收听。

### 26/09/12

（4）增加 JSON 导入/导出同步机制，手机、电脑可以轻松备份。

（5）修复笔记卡片交互：有笔记的卡片默认展开笔记；点击答案区时，答案与笔记一同显示；点击笔记图标只显示笔记，不显示答案。

（6）接入 AI 答疑助手，点击卡片时可自动识别卡片内容并回答。

（7）通过在 AI 系统提示词中设定编号样式，使分点回答不再空行；并优化 JSON 同步，使 AI 配置除 key 外都可同步。

### 26/09/13

（8）修复 AI 分点回答存在空行、间距偏大的问题。

（9）修复 AI 读卡片时只能看到题目和答案区，看不到笔记区和卡片上方自带小标签的问题。

（10）AI 思考过程中，按钮换成浅鹅黄色。

（11）修复手机端 AI 问题：微信内可正常使用，但不能拖拽为独立窗口；手机浏览器打开时无法显示设置卡片，也没有退出功能。

（12）微信无法导出 JSON，必须用手机浏览器打开才能下载；已增加提示和复制按钮。

（13）AI 设计：在复制按钮旁增加"重新生成"按钮和"删除"按钮，可在界面删除对话。

（14）AI 记忆轮数从 6 轮更新至 10 轮。

（15）退出 AI 界面后重新点回，仍停留在刚才的界面，而不是最后的位置。

（16）AI 界面增加类似 DeepSeek 的进度条，方便拖动查看某轮问答；同时处理 AI 输出过快导致手机界面上下横跳的问题。

（17）自动读取卡片时，切换卡片后记忆容易停留在上一道，需要引导才能回答下一道。现已更换为以卡片标签为索引识别，点击新题可直接基于本题回答。

（18）新增图片与附件上传功能。

（19）修复手机端 AI 提问框提示字符溢出问题。

（20）修复微信窗口复制 json 文件超出剪贴板记忆上限问题，已改成分段切分复制粘贴，并进行了统一的 json 文件压缩。

（21）修复同时打开笔记区和 AI 提问区粘贴图片会粘贴两边的问题。

（22）修复关于 AI 重新生成按钮在超过一定时间或轮次不再显示的问题。

### 26/09/14（开始由Workbuddy修复）

（23）修复有的数字旁边有 `$200$` 这样货币符号的问题。

（24）微信浏览器在删掉两条以上的对话内容时，提示框有变化。本来是左边取消、右边确定，但是第三次操作就会出现左边是确定、右边是关闭网页，甚至"关闭网页"这四个字还是绿色的。已经统一全部改成自定义的卡片样式，暂定为左边为红色删除，右边取消。

（25）在导出数据加一个可以选择界面，选择是否导出 AI 聊天。

（26）微信切分感觉 4000 还少了，复制一个切分为 4000 的页面，最多显示 3818，而更多是 3600+ 的。已规定到 4400，帮助减少一些剪切次数。

（27）修复手机端 enter 键发送的问题，调整为换行，电脑端才是 enter 发送。

（28）AI 回答生成过程中，要求可以暂停，点击暂停后消息框下方有继续生成按钮，点击发送键也可再继续生成；此时输入框重新提问会截断上一次对话并进行新的回答。

（29）修复手机端点完"清除""同步"按钮后一直"粘"住不放的问题。

（30）主页“清除”“同步”按钮：外圈光晕粗细统一（手机夜间模式下尤其明显），并去掉了点下去先放大再缩小的动效。

（31）子页笔记区改成点开只看、点文本框才编辑，空笔记和有笔记的卡片行为一致。

（32）AI 窗口右侧进度条长对话可滚动，早期消息能点回去。

（33）AI 对话可以分享：勾选哪几条就只把哪几条生成链接；打开后是铺满整屏的只读窗口（手机电脑自适应），只能复制，不能重新生成或继续提问，不消耗 tokens；链接在转发中被截断时打开会提示；对方直接进对话界面，看不到整站内容，且默认从头看；不再弹出“是否保存密码”。

（34）分享框里的按钮（取消等）加上了和“全选 / 生成链接”一致的悬停反馈，颜色不再发白看不清。

（35）八个页面统一 favicon 图标；标签页标题去掉“南开经济学 · ”前缀，统一为“微观背诵 · 记忆卡”这样的格式。

（36）八个页面都加了“自测模式”开关：答案先遮住自己背，点“查看答案”对照，再自评“会了／模糊／忘了”，点同一个键可清除标记；统计条按标记筛选，答案能重新遮住反复默写；遮罩本身点不动，防手机误触；关掉开关后外观、按钮、提示全部恢复原样。历年真题页没有答案区，改用“错题本”模式，只做掌握度标记和筛选，作答区照常使用。

（37）笔记与图片：插入的图片自动压缩后保存；笔记自动保存，写一半关掉页面会提醒；浏览器存储不够时明确提示“没保存成功”；清空笔记的确认弹窗换成了和删除对话、同步那里一致的自定义卡片样式。

（38）手机端在 AI 窗口里上下滑动看上下文，不会再把整个页面带着滚，退出后正在看的卡片位置不变。

### 26/09/15

（39）自测模式下，答案没遮住时 AI 能读到，遮住时读不到；AI 会锁定当前正在看的那张卡片。

（40）修掉了手机端“点一下按钮样式粘住、必须点旁边才恢复”的问题：把 `:hover` 规则收进只在电脑生效的媒体查询，触摸端改成 `:active` 按下反馈，松手立刻复原。

（41）收藏、笔记、编辑、插图、保存、清空这几个按钮的事件处理器接回正确位置；清空弹窗只在点“🗑️ 清空”时出现。

（42）小于号和转义：数据统一存裸字符 `<` `>`，显示时统一转义；修掉编辑时 `<` 被当成 HTML 标签吞内容、页面显示 `&lt;`、搜索 `<` 搜不全的问题。

（43）关于“**”：只有“计算要点”页的 `**` 是真正的加粗标记，其余页面里的 `**` 全是数学记号（`w**`、`X**`），原样显示是对的，不要推广加粗渲染。

（44）筛选下拉框改成从数据自动生成，以后加章节 / 加专题只要改数据，不用再手改 HTML 里的选项列表。没在显示名里列到的值会按原值出现在最前面。

（45）配置里的 API Key 输入框改成普通文本框 + CSS 圆点遮罩（看起来还是像密码框），浏览器不再当它是登录凭据、不再弹“保存密码”。

（46）结构上的四处整理：搜索框加防抖（打字过程中不重画，停下来才画一次）；卡片上的按钮改成事件委托，只绑一次，不再每次重画都重新绑；确认弹窗抽成 `shared/confirm.js`；“存储安全”（图片压缩 + 笔记自动保存 + 离开提醒）抽成 `shared/storage-guard.js`。

（47）KaTeX 公式库改成本地 `vendor/katex/`，不再走 CDN，万一 `vendor/` 没传上去会自动兜底回 CDN；取消编辑的提示图标换成 🔙。

（48）PWA：九个页面补齐图标与 `manifest.webmanifest` 声明，手机上可装到主屏当应用用（需要 HTTPS + 用网址打开）。图标定为浅紫底五瓣花，方角满幅、四角不留白；应用名和页面标题保持“南开经济学 · 知识库”不变；主题色南开紫 `#6A2C91`。

### 26/09/16

（49）手机端朗读：暂停后再点播放能接着往下读（按标点分句 + 断点续播），电脑端不变。

（50）收藏与蓝色卡片的配色统一：收藏后的卡片是米白底配金色左边框，暖色只保留在标签、题号、答案区和“点击展开答案”，题目与答案正文一律近黑；蓝色卡片未收藏为淡蓝白，收藏后同样转米白；真题页说明卡学科标签蓝底，题目区文字为黑。

（51）论述专项的序号带字母前缀：使用说明卡 `U1…U8`，其余卡 `Q1、Q2…`。

（52）AI 助手的进度卡片：以整个 AI 窗口为基准垂直居中，最高占窗口一半，内容在卡片内滚动。

### 26/09/17（本次由无思微程序修复）

（53）筛选下拉框动效调整：点击时重新显示绿色光圈动效，选中后光圈自动消失；搜索框光圈照旧保留。

（54）分享链接变短：分享内容改用紧凑格式 + UTF-16 打包 + base64url 编码，同样内容的链接长度大约缩短三分之一以上，转发时更不容易被截断；旧版链接仍然可以打开。

（55）修复“有笔记的卡片点📝仍会跳进编辑”的间歇问题：现在点📝只打开查看、不再自动聚焦；保存笔记或点出编辑框都会回到查看态。

（56）分享升级：分享栏按钮由“生成链接”改为“生成分享”，点击后可二选一——“生成链接”或“生成图片保存”。图片在本地生成，不经过任何服务器。

（57）分享图片预览：点“保存图片”后，保存成功时预览面板自动关闭，保存失败时仍保留预览方便重试或长按另存；“保存图片”按钮去掉图标；分享图片顶部和署名简化。

（58）勾选分享的稳定性：修复偶发的“勾选后点生成分享却提示无可分享内容”；现在重画会原样保留勾选状态，处于勾选模式时再点“分享”也会自动补齐选择框与选择条计数。

### 26/09/20（继续由Workbuddy修复）

（59）新增第九个页面“我的卡片”：自己新建卡片，题目、答案分开写，答案区是富文本（加粗、斜体、下划线、图片、表格、行内公式、块级公式），可以收藏、按章节归类、按拼音排序，顶部两颗小球分别管“清空数据”和“导入 / 导出 JSON”。

（60）八个模块的笔记区统一升级成富文本工具栏：B 加粗、I 斜体、U 下划线、🧹 清除格式、🖼️ 插入图片、▦ 插入表格、∑ 行内公式、∑ 块级公式、💾 保存、🗑️ 清空。电脑端显示完整文案，手机端自动换成短文案，窄屏两行排得下。历年真题页没有答案区，工具栏挂在作答区 / 笔记区上。

（61）笔记里的图片：编辑态点图片弹出像素尺寸条，能看到原图尺寸和体积，拖动即可预览，点“确认压缩”是真的把图片重新采样压小；宽高比始终锁定，滑块上限就是笔记框的整行宽。非编辑态点图片打开浮层预览，可以放大、缩小、1:1、适应窗口。

（62）笔记区和答案区都能写公式：行内写 $…$，独占一行写 $$…$$，保存后渲染成公式；也可以直接粘贴截图。

（63）主页新增“我的卡片”通栏入口；导出 / 同步时把“我的卡片”里的卡片和章节一起打包。

### 26/09/21（本版新增）

（64）AI 答疑助手的聊天窗里加了两个通往“我的卡片”的入口：右上角卡片图标新开标签页打开“我的卡片”；每条 AI 回答右下角多一枚“存成卡片”，把“提问 + 这条回答”直接存成卡片，提问当题目、回答当答案，Markdown 自动转成富文本表格和公式，同一条不会重复存。

（65）换成新版系统提示词：只讲微观、宏观两门课，按“概念 / 推导 / 计算 / 对比 / 答题”五类组织答法，公式一律 LaTeX、对比一律表格、分点用中文编号，遇到高频考点会主动提醒背结论。老配置自动升级。

（66）“我的卡片”新建卡片时，答案区的提示简化为“富文本 · 表格 · 公式 · 图片”。

（67）“存成卡片”加确认弹窗：点按钮先弹出题目和答案预览，点“存入”才存，点“取消”不写入；成功提示去掉前面的 emoji。

（68）AI 聊天窗手机端的对话进度条改成和电脑端同一套：横条列高度固定，上下都留边距，点一下弹出小卡片、每行显示对应提问，点某一行直接跳到那一问。

（69）修复分享勾选的一串问题：勾选计数显示错、生成分享提示没勾选；现在每条消息都有编号，删除 / 重新生成后勾选自动修正，“全选 / 取消全选”按钮文字与实际一致。

（70）修复“设置”按钮来回切换后对话乱跳的问题：从设置页回到聊天，对话停在离开时的位置，进度条高亮也停在原来的那条。

（71）正在生成回答时关掉 AI 窗口，不再把已生成的内容丢掉：会自动把已收到的部分存成一条完整回答，重新打开还能继续看。

（72）修复“自测模式开关看着是开的、实际还是原模式”的问题：页面放置一段时间后重新回来，开关状态会和实际模式对齐。

（73）公式显示更稳：KaTeX 还没加载完时，公式一就绪立刻把页面上残留的 $…$ 源码重新排成公式，基本看不到黄色未渲染状态。

（74）修复子页笔记区插入表格跑到最前面的问题：无记录时自动把光标落到编辑器末尾，表格 / 图片 / 公式都插在合理位置。

（75）“我的卡片”新建卡片时记住上一次使用的章节：保存后下次新建同分类卡片默认选中，切换分类各自独立记忆，章节被删后回退到空。

（76）AI“存成卡片”按钮三处调整：① 现场流式输出的回答生成后立刻出现按钮；② 未保存时 hover 不变绿，和另外三个按钮一样保持中性，保存后才变绿；③ 已保存的回答再点一下弹出“移出卡片”确认框，确认后从“我的卡片”中移除。

（77）AI“存成卡片”按钮视觉与弹窗再调：未保存时恢复白底、和另外三个按钮完全一致的悬停阴影，保存后才变绿；确认弹窗在电脑端加宽到 560px，答案预览区更高。

（78）子页富文本表格插入再加固：点击“表格”弹窗前先把真实选区记下来，插入时优先用这个记录位置，杜绝内容被插到最前面；表格后面自动跟一个空段落，光标直接放进去，用户可以直接回车/打字换行。

（79）子页富文本表格可删除：光标落在表格后的空段落时按 Backspace、或选中整张表时按 Backspace/Delete，都会删掉整张表；删除走 execCommand，Ctrl+Z 可以撤销，单元格内部正常删文字不受影响。

---

## 四、仍存在的问题

### 26/09/13

（1）手机夜间模式主页"清除"和"同步"按钮存在外圈光晕粗细不一致的问题，应该是因为颜色反色的自带情况，没有做夜间模式适配。但不是很需要改，因为我一般不用夜间模式学习。
